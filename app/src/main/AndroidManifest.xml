<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mfanir.ecommerceapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Admin.Order.OverviewUserOrderActivity"></activity>
        <activity android:name=".Admin.Product.ProductAdminDetailActivity" />
        <activity android:name=".Admin.Order.UserOrderDetailActivity" />
        <activity android:name=".Admin.Product.SelectCategory" />
        <activity android:name=".User.Cart.ConfirmFinalOrderActivity" />
        <activity android:name=".User.Dashboard.ProductDetailActivity" />
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".User.Setting.UpdateProfileActivity" />
        <activity
            android:name=".User.HomeActivity"
            android:label="@string/title_activity_home"
            />
        <activity android:name=".Admin.AdminHomeActivity" />
        <activity android:name=".Admin.Product.AddProductActivity" />
        <activity android:name=".Auth.VerifyPhoneActivity" />
        <activity android:name=".Auth.SignUpActivity" />
        <activity android:name=".Auth.LoginActivity" />
        <activity android:name=".MainActivity" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>